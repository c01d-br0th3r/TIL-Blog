export interface IData {
  id: number;
  title: string;
  content: string;
  summary: string;
  thumbnail: string;
  tags: string;
  created_at: string;
  postNumber: string;
}

export interface IMatch {
  id: string;
}
